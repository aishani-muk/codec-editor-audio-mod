"""Interactive Gradio demo for the codec-to-codec raga editor.

Subpackages
-----------
profiles : u(t) stress-intensity generators (flat, ramp, pulse, arch,
           sine, keyframe).
runner   : Time-varying inference wrapper over the DSP baseline,
           EnCodec+MLP baseline, and the proposed codec editor.
app      : Gradio interface (run with `python -m demo.app`).
"""
